Status-nginx-module debian builds
=================================

This is least-effort modification of debian nginx (obtained by `apt-get source nginx`) to build
just this module `libngninx-mod-http-status`.


